import 'package:flutter/material.dart';

/// AnimeCard class
class AnimeCard extends StatelessWidget {
  AnimeCard({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AnimeCard')
      ),
      body: Container(),
    );
  }
}
